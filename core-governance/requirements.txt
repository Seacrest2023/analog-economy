# Analog Economy - Core Governance Dependencies

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Data Validation
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Configuration
pyyaml>=6.0.1

# HTTP Client (async)
httpx>=0.26.0

# JSON (fast)
orjson>=3.9.0

# Observability (JSON Structured Logging)
structlog>=24.1.0  # ALIGNED: Matches OBSERVABILITY.md standards

# DateTime
pendulum>=3.0.0

# Database (Phase 2 placeholder)
# sqlalchemy>=2.0.0
# asyncpg>=0.29.0

# Testing (The Defense Grid)
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-split>=0.8.0   # CI sharding for parallel testing
mutmut>=2.4.0         # Mutation testing

# Architecture Governance
import-linter>=2.0.0  # ALIGNED: Enforces "Core cannot import API" rules

# Development & Governance
ruff>=0.4.0           # Fast linter + formatter
mypy>=1.8.0           # Static Type Checking
pylint>=3.0.0         # Sniper Mode (Duplicate Code)
types-pyyaml>=6.0.0   # Mypy stubs
